# Pinned for Streamlit Cloud deployment.
# Streamlit Cloud installs from this whenever a deploy points at this repo,
# regardless of which subpath holds the entry-point app.py.

anthropic>=0.40
pydantic>=2.7
pyyaml>=6.0
typer>=0.12
rich>=13.7
httpx>=0.27
streamlit>=1.39
pandas>=2.1
rank-bm25>=0.2.2
chromadb>=0.5

# Install ALL five local packages so any subproject's app.py can deploy
# from this monorepo root.
-e ./octagon
-e ./simulacrum
-e ./festival
-e ./priorauth
-e ./earningscall
