# Pinned for Streamlit Cloud deployment.

anthropic>=0.40
pydantic>=2.7
pyyaml>=6.0
typer>=0.12
rich>=13.7
httpx>=0.27
streamlit>=1.39
pandas>=2.1

# Install the local octagon package so `import octagon` works in the deployed env.
-e .
