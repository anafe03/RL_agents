# Pinned for Streamlit Cloud deployment.
# Streamlit Cloud installs from this whenever a deploy points at this repo,
# regardless of which subpath holds the entry-point app.py.

anthropic>=0.40
pydantic>=2.7
pyyaml>=6.0
typer>=0.12
rich>=13.7
httpx>=0.27
streamlit>=1.39
pandas>=2.1

# Install BOTH local packages so either app's imports resolve in the
# deployed environment. `-e ./simulacrum` follows the subdirectory layout.
-e .
-e ./simulacrum
