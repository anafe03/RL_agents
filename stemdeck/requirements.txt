# Pinned for Streamlit Cloud deployment (catalog browsing + mix simulation
# only — live Ableton control needs python-osc + a local rig, see README).

anthropic>=0.40
pydantic>=2.7
pyyaml>=6.0
typer>=0.12
rich>=13.7
streamlit>=1.39

-e .
